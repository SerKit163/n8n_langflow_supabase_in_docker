# n8n + Langflow + Supabase Stack

Полнофункциональный стек для автоматизации рабочих процессов с AI агентами и базой данных.

## 🚀 Возможности

- **n8n** - автоматизация рабочих процессов и интеграций
- **Langflow** - создание и управление AI агентами
- **Supabase** - PostgreSQL база данных с REST API
- **Ollama** - локальные LLM модели (опционально, требует GPU)
- **Caddy** - автоматический SSL/TLS через Let's Encrypt

## 📋 Требования

- Docker и Docker Compose
- Python 3.8+
- Минимум 4 GB RAM на VPS (рекомендуется 8+ GB)
  - N8N: минимум 1GB, рекомендуется 2-4GB
  - Langflow: минимум 4GB, рекомендуется 4-8GB (для работы с AI агентами)
  - Supabase: минимум 0.5GB, рекомендуется 1-2GB
  - Ollama: минимум 2GB, рекомендуется 4-8GB (для GPU версии)
- Минимум 20 GB свободного места на диске

## ⚠️ ВАЖНО: Лимиты Let's Encrypt

**Let's Encrypt имеет строгие лимиты на количество SSL сертификатов:**

- **50 сертификатов в неделю** на один домен
- **300 сертификатов за 3 часа** на один аккаунт (email)
- **5 продлений** на сертификат в неделю
- **5 ошибок проверки** на домен в час

**Рекомендации:**
- Не переустанавливайте систему слишком часто
- Используйте один и тот же email для всех установок
- Если лимит превышен, нужно ждать **7 дней** до сброса
- Для тестирования используйте staging-среду (не доверяется браузерами)
- Если нужен быстрый доступ без ожидания - используйте режим портов (без SSL)

## 🛠️ Быстрая установка

### 1. Клонируйте репозиторий

```bash
git clone https://github.com/SerKit163/n8n_langflow_supabase_in_docker.git
cd n8n_langflow_supabase_in_docker
```

### 2. Установите Python зависимости

```bash
pip3 install -r requirements.txt
```

### 3. Запустите установщик

```bash
python3 setup.py
```

Установщик автоматически:
- Определит характеристики вашего железа
- Предложит оптимальные настройки
- Настроит домены (поддомены или пути)
- Настроит аутентификацию для Langflow и Supabase
- Сгенерирует все конфигурационные файлы
- Запустит сервисы

## 🌐 Режимы маршрутизации

### 1. Поддомены (рекомендуется для продакшена)
- `n8n.yourdomain.com`, `langflow.yourdomain.com`, `supabase.yourdomain.com`
- SSL сертификаты (HTTPS)
- Безопасное соединение
- ⚠️ Требует домен и может быть лимит на сертификаты

### 2. Пути
- `yourdomain.com/n8n`, `yourdomain.com/langflow`, `yourdomain.com/supabase`
- SSL сертификаты (HTTPS)
- Безопасное соединение
- ⚠️ Требует домен и может быть лимит на сертификаты

### 3. Только порты (для разработки)
- `http://localhost:5678` (N8N)
- `http://localhost:7860` (Langflow)
- `http://localhost:3000` (Supabase Studio)
- Работает сразу, без ожидания
- Не требует домен
- Нет лимитов на сертификаты
- ⚠️ Без SSL (только HTTP)

## 🔐 Аутентификация

### Langflow
При установке автоматически создается суперпользователь:
- Логин: `admin` (или указанный вами)
- Пароль: автоматически сгенерирован (сохраните его!)
- Секретный ключ: автоматически сгенерирован

### Supabase Studio
При установке настраивается:
- Логин: `admin` (или указанный вами)
- Пароль: автоматически сгенерирован (сохраните его!)

## 🔄 Обновление

### Обновление проекта с GitHub

```bash
python3 update_from_github.py
```

Этот скрипт:
- ✅ Обновит код с GitHub (git pull)
- ✅ Сохранит ваши настройки из .env
- ✅ Перегенерирует docker-compose.yml и Caddyfile
- ✅ Перезапустит сервисы с новой конфигурацией
- ✅ Сохранит все ваши данные (volumes)

### Обновление версий Docker образов

```bash
python3 update.py
```

## 🔧 Управление сервисами

### Добавление сервисов

```bash
# Добавить N8N
python3 add_n8n.py

# Добавить Langflow
python3 add_langflow.py

# Добавить Ollama
python3 add_ollama.py
```

### Удаление сервисов

```bash
# Удалить N8N
python3 remove_n8n.py

# Удалить Langflow
python3 remove_langflow.py

# Удалить Ollama
python3 remove_ollama.py
```

**Внимание:** Все данные сервиса будут удалены безвозвратно!

### Диагностика и восстановление

```bash
# Комплексная диагностика и автоматическое восстановление
python3 diagnose_and_fix.py

# Менеджер сервисов
python3 service_manager.py
```

## 🐛 Troubleshooting

### Проблемы с SSL сертификатами

**Если лимит Let's Encrypt превышен:**

1. **Используйте staging-среду для тестирования:**
   ```bash
   python3 switch_to_letsencrypt_staging.py
   python3 regenerate_caddyfile.py
   docker-compose restart caddy
   ```
   ⚠️ Staging сертификаты НЕ доверяются браузерами!

2. **Используйте режим портов (без SSL):**
   ```bash
   python3 switch_to_ports.py
   docker-compose up -d
   ```

3. **Подождите 7 дней** до сброса лимита

**Если сертификаты не выдаются:**

```bash
# Перегенерировать Caddyfile
python3 regenerate_caddyfile.py

# Перезапустить Caddy
docker-compose restart caddy

# Проверить логи
docker-compose logs caddy
```

### Langflow не работает

```bash
# Проверить логи
docker-compose logs langflow

# Проверить права доступа
docker-compose exec langflow ls -la /app/data

# Перезапустить
docker-compose restart langflow
```

### Создание пользователя Langflow через CLI

```bash
python3 create_langflow_user.py username password
```

## 📁 Структура проекта

```
n8n_langflow_supabase_in_docker/
├── setup.py              # Интерактивный установщик
├── update.py             # Скрипт обновления версий Docker образов
├── update_from_github.py # Скрипт обновления проекта с GitHub
├── add_*.py              # Скрипты добавления сервисов
├── remove_*.py           # Скрипты удаления сервисов
├── diagnose_and_fix.py   # Комплексная диагностика
├── service_manager.py    # Менеджер сервисов
├── create_langflow_user.py # Создание пользователя Langflow
├── switch_to_*.py       # Скрипты переключения режимов
├── docker-compose.yml    # Docker Compose конфигурация (генерируется)
├── .env                  # Переменные окружения (генерируется)
├── Caddyfile             # Конфигурация Caddy (генерируется)
├── installer/            # Модули установщика
└── templates/            # Шаблоны конфигураций
```

## 📝 Лицензия

MIT

## 🤝 Вклад

Приветствуются Pull Requests и Issues!

## 📧 Поддержка

Если у вас возникли вопросы, создайте Issue в репозитории.
