# n8n + Langflow + Supabase Stack

Полнофункциональный стек для автоматизации рабочих процессов с AI агентами и базой данных.

## 🚀 Возможности

- **n8n** - автоматизация рабочих процессов и интеграций
- **Langflow** - создание и управление AI агентами
- **Supabase** - PostgreSQL база данных с REST API
- **Ollama** - локальные LLM модели (опционально, требует GPU)
- **OpenRouter** - подключается через API в сервисах (не требует настройки в установщике)

## 📋 Требования

- Docker и Docker Compose
- Python 3.8+
- Минимум 4 GB RAM на VPS (рекомендуется 8+ GB)
  - N8N: минимум 1GB, рекомендуется 2-4GB
  - Langflow: минимум 4GB, рекомендуется 4-8GB (для работы с AI агентами)
  - Supabase: минимум 0.5GB, рекомендуется 1-2GB
  - Ollama: минимум 2GB, рекомендуется 4-8GB (для GPU версии)
- Минимум 20 GB свободного места на диске

## 🛠️ Быстрая установка

### 1. Клонируйте репозиторий

```bash
git clone https://github.com/SerKit163/n8n_langflow_supabase_in_docker.git
cd n8n_langflow_supabase_in_docker
```

### 2. Установите Python зависимости

```bash
pip3 install -r requirements.txt
# или
python3 -m pip install -r requirements.txt
```

### 3. Запустите установщик

```bash
python3 setup.py
```

> **Примечание:** Если команда `python` не найдена, используйте `python3`. 
> Или установите пакет `python-is-python3` для создания симлинка:
> ```bash
> sudo apt install python-is-python3
> ```

Установщик автоматически:
- Определит характеристики вашего железа
- Предложит оптимальные настройки
- Настроит домены (поддомены или пути)
- Сгенерирует все конфигурационные файлы
- Запустит сервисы

## 🔄 Обновление

### Обновление проекта с GitHub (рекомендуется)

Обновляет код проекта с GitHub и перегенерирует конфигурационные файлы с сохранением ваших настроек:

```bash
python3 update_from_github.py
```

Этот скрипт:
- ✅ Обновит код с GitHub (git pull)
- ✅ Сохранит ваши настройки из .env
- ✅ Перегенерирует docker-compose.yml и Caddyfile
- ✅ Перезапустит сервисы с новой конфигурацией
- ✅ Сохранит все ваши данные (volumes)

### Обновление версий Docker образов

Обновляет только версии Docker образов сервисов:

```bash
python3 update.py
```

Скрипт:
- Проверит текущие версии
- Найдет доступные обновления (для образов с конкретными версиями)
- Покажет прогресс загрузки образов
- Обновит сервисы

**Примечание:** Образы с тегом `latest` всегда считаются актуальными и не предлагаются к обновлению.

### Восстановление версий после неправильного обновления

Если после обновления сервисы перестали работать из-за неправильных версий:

```bash
python3 restore_versions.py
```

Этот скрипт:
- Восстановит правильные версии Docker образов в `docker-compose.yml`
- Перезапустит сервисы с корректными версиями

## 🤖 Управление Ollama

### Добавление Ollama

Если вы хотите добавить Ollama к уже установленной системе:

```bash
python3 add_ollama.py
```

Этот скрипт:
- ✅ Проверит существующую конфигурацию
- ✅ Определит наличие GPU/CUDA для выбора версии
- ✅ Настроит домен/путь/порт в зависимости от режима маршрутизации
- ✅ Обновит .env, docker-compose.yml и Caddyfile
- ✅ Автоматически запустит Ollama контейнер
- ✅ Покажет информацию о доступе

**Примечания:**
- Для GPU версии нужна NVIDIA GPU с CUDA
- Без GPU будет использована CPU версия (работает медленнее)
- Ollama требует много памяти (минимум 2GB, рекомендуется 4-8GB)
- После установки можно скачать модели: `docker exec -it ollama ollama pull llama2`

### Удаление Ollama

Если вам больше не нужен Ollama и вы хотите освободить место на диске:

```bash
python3 remove_ollama.py
```

Этот скрипт:
- ✅ Остановит и удалит контейнер Ollama
- ✅ Удалит volume с данными Ollama (модели) - освободит место
- ✅ Удалит Ollama из .env, docker-compose.yml и Caddyfile
- ✅ Перезапустит остальные сервисы

**Внимание:** Все данные Ollama (модели) будут удалены безвозвратно!

**Дополнительно:** Скрипт также предложит удалить неиспользуемый Docker образ Ollama для освобождения места на диске. Размер образа будет показан перед удалением.

## 🔧 Управление N8N и Langflow

### Добавление N8N

Если вы хотите добавить N8N к уже установленной системе:

```bash
python3 add_n8n.py
```

Этот скрипт:
- ✅ Проверит существующую конфигурацию
- ✅ Настроит домен/путь/порт в зависимости от режима маршрутизации
- ✅ Настроит ресурсы (память, CPU)
- ✅ Обновит .env, docker-compose.yml и Caddyfile
- ✅ Автоматически запустит N8N контейнер
- ✅ Покажет информацию о доступе

**Примечания:**
- N8N требует минимум 1GB RAM (рекомендуется 2-4GB)
- При первом запуске N8N создаст учетную запись администратора

### Добавление Langflow

Если вы хотите добавить Langflow к уже установленной системе:

```bash
python3 add_langflow.py
```

Этот скрипт:
- ✅ Проверит существующую конфигурацию
- ✅ Настроит домен/путь/порт в зависимости от режима маршрутизации
- ✅ Настроит ресурсы (память, CPU) с учетом требований Langflow
- ✅ Обновит .env, docker-compose.yml и Caddyfile
- ✅ Автоматически запустит Langflow контейнер
- ✅ Покажет информацию о доступе

**Примечания:**
- Langflow требует много памяти (минимум 4GB, рекомендуется 4-8GB)
- Для работы с AI агентами рекомендуется минимум 8GB RAM на VPS
- При первом запуске Langflow может занять несколько минут для инициализации
- Если страница не загружается, проверьте логи: `docker-compose logs langflow`
- Langflow автоматически настраивает права доступа к директориям данных

### Удаление N8N

Если вы хотите удалить N8N из установки:

```bash
python3 remove_n8n.py
```

Этот скрипт:
- ✅ Остановит и удалит контейнер N8N
- ✅ Удалит volume с данными N8N (workflows, credentials) - освободит место
- ✅ Удалит N8N из .env, docker-compose.yml и Caddyfile
- ✅ Перезапустит остальные сервисы

**Внимание:** Все данные N8N (workflows, credentials) будут удалены безвозвратно!

**Дополнительно:** Скрипт также предложит удалить неиспользуемый Docker образ N8N для освобождения места на диске. Размер образа будет показан перед удалением.

### Удаление Langflow

Если вы хотите удалить Langflow из установки:

```bash
python3 remove_langflow.py
```

Этот скрипт:
- ✅ Остановит и удалит контейнер Langflow
- ✅ Удалит volume с данными Langflow (flows, компоненты) - освободит место
- ✅ Удалит Langflow из .env, docker-compose.yml и Caddyfile
- ✅ Перезапустит остальные сервисы

**Внимание:** Все данные Langflow (flows, компоненты) будут удалены безвозвратно!

**Дополнительно:** Скрипт также предложит удалить неиспользуемый Docker образ Langflow для освобождения места на диске. Размер образа будет показан перед удалением.

## ⚙️ Ручная установка

Если вы предпочитаете ручную установку:

1. Скопируйте `.env.example` в `.env` и заполните переменные
2. Запустите `docker-compose up -d`

## 🌐 Настройка доменов

### Режим поддоменов

Установите в `.env`:
```env
ROUTING_MODE=subdomain
N8N_DOMAIN=n8n.yourdomain.com
LANGFLOW_DOMAIN=langflow.yourdomain.com
SUPABASE_DOMAIN=supabase.yourdomain.com
```

### Режим путей

Установите в `.env`:
```env
ROUTING_MODE=path
BASE_DOMAIN=yourdomain.com
N8N_PATH=/n8n
LANGFLOW_PATH=/langflow
SUPABASE_PATH=/supabase
```

### Без доменов (разработка)

Оставьте `ROUTING_MODE` пустым и используйте порты:
- N8N: http://localhost:5678
- Langflow: http://localhost:7860
- Supabase: http://localhost:8000

## 🎯 Примеры использования

### Создание автоматизации в n8n

1. Откройте n8n в браузере
2. Создайте новый workflow
3. Добавьте HTTP Request node для вызова Langflow API
4. Настройте интеграцию с Supabase для сохранения данных

### Создание AI агента в Langflow

1. Откройте Langflow в браузере
2. Создайте новый flow
3. Используйте Ollama или OpenRouter для LLM
4. Настройте RAG с Supabase

### Интеграция с Google Calendar

Пример workflow в n8n:
- Триггер: Webhook от Langflow агента
- Действие: Создание события в Google Calendar
- Сохранение в Supabase

## 🔧 Управление сервисами

### Диагностика и автоматическое восстановление (рекомендуется)

Комплексный скрипт для диагностики всех компонентов системы и автоматического исправления проблем:

```bash
python3 diagnose_and_fix.py
```

**Возможности:**
- ✅ Полная диагностика всех сервисов (n8n, langflow, supabase, caddy)
- ✅ Проверка подключения к базе данных
- ✅ Проверка схемы auth и необходимых типов
- ✅ Проверка volumes и сетей
- ✅ Автоматическое исправление найденных проблем
- ✅ Ручное исправление конкретных проблем
- ✅ Просмотр логов проблемных сервисов
- ✅ Подробная сводка с рекомендациями

**Пример использования:**
1. Запустите `python3 diagnose_and_fix.py`
2. Скрипт автоматически проверит все компоненты
3. Выберите действие:
   - **Автоматическое исправление** - скрипт сам исправит все проблемы
   - **Ручное исправление** - выберите конкретные проблемы
   - **Просмотр логов** - посмотрите логи проблемных сервисов
4. После исправлений скрипт проведет повторную диагностику

**Важно:** Скрипт безопасен - он не удалит данные без вашего подтверждения!

### Менеджер сервисов

Интерактивный скрипт для управления отдельными сервисами с сохранением всех данных:

```bash
python3 service_manager.py
```

**Возможности:**
- ✅ Просмотр статуса всех сервисов
- ✅ Перезапуск отдельного сервиса (быстро)
- ✅ Пересоздание контейнера с сохранением данных в volumes
- ✅ Просмотр логов проблемного сервиса
- ✅ Проверка здоровья сервисов
- ✅ Автоматическое восстановление проблемных сервисов

**Пример использования:**
1. Запустите `python3 service_manager.py`
2. Выберите "6. Восстановить проблемный сервис"
3. Введите имя сервиса (например, `langflow`)
4. Скрипт покажет статус, логи и предложит варианты восстановления
5. Выберите действие (перезапуск или пересоздание)

**Важно:** Все данные сохраняются в Docker volumes, поэтому пересоздание контейнера безопасно!

### Запуск всех сервисов

```bash
docker-compose up -d
```

### Остановка всех сервисов

```bash
docker-compose down
```

### Просмотр логов

```bash
docker-compose logs -f [service_name]
```

### Перезапуск отдельного сервиса

```bash
docker-compose restart [service_name]
```

### Пересоздание сервиса (с сохранением данных)

Если сервис завис или работает некорректно, можно пересоздать контейнер:

```bash
# Остановить и удалить контейнер (данные в volume сохраняются)
docker-compose stop [service_name]
docker-compose rm -f [service_name]

# Пересоздать контейнер
docker-compose up -d [service_name]
```

Или используйте менеджер сервисов для автоматического восстановления.

## 📊 Мониторинг ресурсов

Установщик автоматически определяет:
- CPU (количество ядер)
- RAM (общий объем VPS - используется для проверки требований)
- GPU (если доступна, NVIDIA с CUDA для Ollama)
- Свободное место на диске

И адаптирует настройки под ваше железо.

**Важно:** Проверка ресурсов использует общую RAM VPS (total_gb), а не доступную в данный момент, так как Docker использует лимиты памяти, а не всю доступную память. Это обеспечивает корректную оценку ресурсов даже на загруженном VPS.

## 🔒 Безопасность

- Все пароли и секреты генерируются автоматически
- SSL/TLS поддержка через Let's Encrypt
- Обход лимитов Let's Encrypt (DNS Challenge, ротация email, TLS on Demand)
- Ограничения ресурсов для каждого сервиса
- Health checks для мониторинга

## 📁 Структура проекта

```
n8n_langflow_supabase_in_docker/
├── setup.py              # Интерактивный установщик
├── update.py             # Скрипт обновления версий Docker образов
├── update_from_github.py # Скрипт обновления проекта с GitHub
├── add_ollama.py         # Скрипт добавления Ollama
├── add_n8n.py            # Скрипт добавления N8N
├── add_langflow.py       # Скрипт добавления Langflow
├── remove_ollama.py      # Скрипт удаления Ollama (с удалением образа)
├── remove_n8n.py         # Скрипт удаления N8N (с удалением образа)
├── remove_langflow.py    # Скрипт удаления Langflow (с удалением образа)
├── diagnose_and_fix.py   # Комплексная диагностика и автоматическое восстановление
├── fix_supabase_db.py    # Исправление проблем с базой данных Supabase
├── service_manager.py    # Менеджер сервисов (восстановление, логи, статус)
├── restore_versions.py   # Восстановление версий Docker образов после неправильного обновления
├── docker-compose.yml    # Docker Compose конфигурация (генерируется)
├── .env                  # Переменные окружения (не коммитится, генерируется)
├── Caddyfile             # Конфигурация Caddy (генерируется)
├── installer/            # Модули установщика
│   ├── hardware_detector.py    # Определение характеристик системы
│   ├── config_adaptor.py        # Адаптация настроек под железо
│   ├── resource_checker.py     # Проверка ресурсов (использует общую RAM VPS)
│   ├── config_generator.py     # Генерация конфигурационных файлов
│   ├── docker_manager.py       # Управление Docker контейнерами
│   ├── version_checker.py     # Проверка версий Docker образов
│   └── utils.py                # Вспомогательные функции
├── templates/            # Шаблоны конфигураций
│   ├── docker-compose.cpu.template  # Docker Compose для CPU
│   ├── docker-compose.gpu.template  # Docker Compose для GPU
│   └── env.template              # Шаблон .env
├── scripts/              # Bash скрипты управления
└── README.md
```

## 🐛 Troubleshooting

### Сервисы не запускаются

1. Проверьте логи: `docker-compose logs`
2. Убедитесь что порты свободны
3. Проверьте достаточно ли ресурсов

### Langflow не работает

- Проверьте логи: `docker-compose logs langflow`
- Убедитесь что порт 7860 свободен
- Проверьте права доступа к volume: `docker-compose exec langflow ls -la /app/data`
- Убедитесь что на VPS достаточно RAM (минимум 4GB для Langflow)
- Проверьте что переменная `LANGFLOW_CONFIG_DIR` установлена в `/app/data/.langflow`
- Если проблемы с правами доступа, контейнер автоматически исправит их при запуске (запускается от root)

### Supabase Studio не требует входа

Supabase Studio - это админ-панель для управления проектами. По умолчанию она не требует входа, если переменные окружения настроены правильно. Studio подключается к базе данных напрямую через переменные `STUDIO_PGUSER` и `STUDIO_PGPASSWORD`.

Если вы хотите настроить вход в Studio, это можно сделать через настройку переменных окружения, но обычно Studio работает без входа для админ-панели.

### Ollama не работает

- Убедитесь что GPU доступна: `nvidia-smi`
- Проверьте что установлен NVIDIA Container Toolkit
- Используйте OpenRouter как альтернативу

### Проблемы с доменами (nginx-proxy)

**Важно:** Домены работают только если:
1. `ROUTING_MODE=subdomain` в файле `.env`
2. Домены указаны в `.env` (например, `N8N_DOMAIN=n8n.example.com`)
3. DNS записи настроены и указывают на IP вашего сервера
4. Порты 80 и 443 открыты в файрволе
5. nginx-proxy контейнер запущен: `docker-compose ps nginx-proxy`

**Проверка работы доменов:**
```bash
# Проверьте, что переменные VIRTUAL_HOST заполнены
docker-compose exec n8n env | grep VIRTUAL_HOST

# Проверьте логи nginx-proxy
docker-compose logs nginx-proxy

# Проверьте, что DNS записи настроены
nslookup n8n.example.com
```

**Если домены не работают:**
1. Убедитесь, что в `.env` указан `ROUTING_MODE=subdomain`
2. Проверьте, что домены указаны в `.env` (не пустые)
3. Проверьте DNS записи - они должны указывать на IP вашего сервера
4. Убедитесь, что порты 80 и 443 открыты
5. Перезапустите контейнеры: `docker-compose restart`

## 📝 Лицензия

MIT

## 🤝 Вклад

Приветствуются Pull Requests и Issues!

## 📧 Поддержка

Если у вас возникли вопросы, создайте Issue в репозитории.

